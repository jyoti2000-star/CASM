%extern stdio.h

%var number 5
%var result 0

%println "Starting C integration test..."

%if number > 0
    %! printf("Number is positive: %d\n", 42);
    %! int temp = number * 2;
    %var result temp
%endif

%println "Test complete!"